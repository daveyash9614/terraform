name: "Master Terraform Base Engine"

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      working_directory:
        required: true
        type: string

jobs:
  # --- JOB 1: Security & Quality Checks ---
  lint-and-scan:
    name: "Quality & Security Scan"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.5

      - name: Validate Formatting
        run: terraform fmt -check -recursive

      - name: Run TFLint
        uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: latest

      - name: Run Checkov Security Scan
        uses: bridgecrewio/checkov-action@master
        with:
          directory: ${{ inputs.working_directory }}
          framework: terraform
          soft_fail: true

  # --- JOB 2: Plan Phase ---
  terraform-plan:
    name: "Plan [${{ inputs.environment }}]"
    needs: lint-and-scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Azure SP Login
        uses: azure/login@v2
        with:
          creds: '{"clientId":"70a65af7-843e-4a9a-b801-75d5231c72f4","clientSecret":"M5c8Q~h0p3Zh3U1Cs.uuUpZWjkET9M0oBx6kgbzu","subscriptionId":"1b17fba3-24d7-46a8-be64-97ad373509ca","tenantId":"453f8321-3cb4-4f15-bf44-4544f4198c66"}'

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.0

      - name: Terraform Init
        run: terraform init
        working-directory: ${{ inputs.working_directory }}

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan \
            -no-color \
            -input=false \
            -var-file="terraform.tfvars" \
            -out=${{ inputs.environment }}.tfplan
        working-directory: ${{ inputs.working_directory }}

      - name: Upload Plan Artifact
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ inputs.environment }}-${{ github.sha }}
          path: ${{ inputs.working_directory }}/${{ inputs.environment }}.tfplan
          retention-days: 7

      - name: Post Plan to PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const env = "${{ inputs.environment }}";
            const output = `### Environment: \`${env}\` 🚀
            #### Terraform Plan Status: \`Success\`
            *Plan Artifact Generated: \`tfplan-${env}-${{ github.sha }}\`*

            *Pusher: @${{ github.actor }}, Event: \`${{ github.event_name }}\`*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })

  # --- JOB 3: Apply Phase ---
  terraform-apply:
    name: "Apply [${{ inputs.environment }}]"
    needs: terraform-plan
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Azure SP Login
        uses: azure/login@v2
        with:
          creds: '{"clientId":"70a65af7-843e-4a9a-b801-75d5231c72f4","clientSecret":"M5c8Q~h0p3Zh3U1Cs.uuUpZWjkET9M0oBx6kgbzu","subscriptionId":"1b17fba3-24d7-46a8-be64-97ad373509ca","tenantId":"453f8321-3cb4-4f15-bf44-4544f4198c66"}'

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.0

      - name: Download Plan Artifact
        uses: actions/download-artifact@v4
        with:
          name: tfplan-${{ inputs.environment }}-${{ github.sha }}
          path: ${{ inputs.working_directory }}

      - name: Terraform Init
        run: terraform init
        working-directory: ${{ inputs.working_directory }}

      - name: Terraform Apply
        run: terraform apply -input=false ${{ inputs.environment }}.tfplan
        working-directory: ${{ inputs.working_directory }}
